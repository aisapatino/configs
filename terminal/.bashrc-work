### INCLUDE BASE BASHRC #######################################################

source ~/Project/configs/terminal/.bashrc-base

### AUTOCOMPLETE ##############################################################

if [ -f `brew --prefix`/etc/bash_completion ]; then
  . `brew --prefix`/etc/bash_completion
fi

